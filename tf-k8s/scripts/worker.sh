#!/bin/bash
set -e

# kubeadm join is intentionally omitted; run it manually once the token is available
echo "kubeadm join command not configured. SSH into this node and run the join command manually."
